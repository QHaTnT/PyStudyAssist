# -*- coding: utf-8 -*-
"""
配置文件 - PyPalPrep Python学习教辅系统

【文件功能说明】
这个文件存储了整个系统的全局配置信息。
就像一个"控制中心"，所有需要修改的参数都集中在这里。

【为什么需要配置文件】
1. 集中管理：所有配置项都在一个地方，方便修改
2. 避免硬编码：不需要在代码中到处写死具体的值
3. 易于维护：修改配置时不需要修改业务逻辑代码

【配置项分类】
1. 路径配置：数据库路径、资源目录等
2. 窗口配置：窗口大小、标题等
3. 主题配置：颜色、字体等
4. 数据配置：知识点分类、题目类型等
"""
import os  # 导入操作系统模块，用于处理文件路径


# ============================================================================
# 路径配置
# ============================================================================

# 项目根目录
# os.path.dirname(os.path.abspath(__file__)) 获取当前文件所在的目录
# 这样无论程序从哪里运行，都能找到正确的路径
BASE_DIR = os.path.dirname(os.path.abspath(__file__))

# 数据库配置
# 数据库文件存储在 database/python_learning.db
# 使用os.path.join()拼接路径，确保跨平台兼容（Windows用\，Linux用/）
DATABASE_PATH = os.path.join(BASE_DIR, 'database', 'python_learning.db')

# 资源目录配置
# RESOURCES_DIR: 资源根目录
# ICONS_DIR: 图标目录
# IMAGES_DIR: 图片目录
# DATA_DIR: 数据文件目录
RESOURCES_DIR = os.path.join(BASE_DIR, 'resources')
ICONS_DIR = os.path.join(RESOURCES_DIR, 'icons')
IMAGES_DIR = os.path.join(RESOURCES_DIR, 'images')
DATA_DIR = os.path.join(RESOURCES_DIR, 'data')


# ============================================================================
# 默认用户配置
# ============================================================================

# 默认用户信息
# 用于数据库初始化和开发测试
DEFAULT_USER = {
    'username': '1',      # 默认用户名
    'password': '1',      # 默认密码（实际存储时会加密）
    'nickname': 'Python学习者'  # 默认昵称
}


# ============================================================================
# 窗口配置
# ============================================================================

# 主窗口配置
WINDOW_CONFIG = {
    'title': 'PyPalPrep',           # 窗口标题
    'width': 1200,                  # 默认宽度（像素）
    'height': 800,                  # 默认高度（像素）
    'min_width': 1000,              # 最小宽度（用户不能缩小到这个尺寸以下）
    'min_height': 600               # 最小高度
}


# ============================================================================
# 代码编辑器配置
# ============================================================================

# 代码编辑器设置
EDITOR_CONFIG = {
    'font_family': 'Consolas',      # 等宽字体，适合显示代码
    'font_size': 12,                # 字体大小（pt）
    'tab_size': 4,                  # Tab键对应的空格数
    'timeout': 5                    # 代码执行超时时间（秒），防止无限循环
}


# ============================================================================
# 主题颜色配置
# ============================================================================

# 主题颜色
# 采用iOS风格的浅色系配色，清新舒适
THEME_COLORS = {
    'primary': '#5B9BD5',      # 主色调：柔和蓝，用于按钮、标题等
    'success': '#6FBF73',      # 成功色：绿色，用于完成、正确等
    'warning': '#E6A23C',      # 警告色：橙色，用于提示、警告等
    'danger': '#E05252',       # 危险色：红色，用于错误、删除等
    'info': '#7FD7E6',         # 信息色：浅蓝色，用于信息提示
    'dark': '#3C4858',         # 深色：用于文字，清晰易读
    'light': '#FFFFFF',        # 浅色：纯白色，用于卡片背景
    'background': '#F6F7F9'    # 背景色：浅灰色，页面背景
}


# ============================================================================
# 数据配置
# ============================================================================

# 知识点分类
# 这些分类对应Python二级考试的知识点分类
KNOWLEDGE_CATEGORIES = [
    'Python基础',           # Python基本概念和语法
    '数据类型',             # 数字、字符串、列表、字典等
    '运算符与表达式',       # 算术、比较、逻辑运算符
    '流程控制',             # if/else、for、while等
    '函数',                 # 函数定义、参数、返回值
    '字符串处理',           # 字符串方法和操作
    '列表与元组',           # 序列类型的操作
    '字典与集合',           # 映射和集合类型
    '文件操作',             # 文件读写操作
    '异常处理',             # try/except异常处理
    '面向对象编程',         # 类、对象、继承等
    '模块与包',             # 模块导入和使用
    '常用标准库'            # random、datetime、math等
]

# 题目类型
# choice: 选择题（单选）
# judge: 判断题（对/错）
# fill: 填空题
# code: 编程题
QUESTION_TYPES = {
    'choice': '选择题',
    'judge': '判断题',
    'fill': '填空题',
    'code': '编程题'
}

# 难度等级
# easy: 简单（基础知识点）
# medium: 中等（需要理解）
# hard: 困难（综合应用）
DIFFICULTY_LEVELS = {
    'easy': '简单',
    'medium': '中等',
    'hard': '困难'
}
